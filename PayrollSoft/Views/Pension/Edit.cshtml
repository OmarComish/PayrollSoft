@model PayrollSoft.Models.Pension

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)
    <fieldset>
        <legend>Pension</legend>

        @Html.HiddenFor(model => model.PensionRefNum)

        <div class="editor-label">
            @Html.LabelFor(model => model.EmployerContrRate)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.EmployerContrRate)
            @Html.ValidationMessageFor(model => model.EmployerContrRate)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.EmployeeContrRate)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.EmployeeContrRate)
            @Html.ValidationMessageFor(model => model.EmployeeContrRate)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
