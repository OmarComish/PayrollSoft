@model PayrollSoft.Models.Pension

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>Pension</legend>

    <div class="display-label">EmployerContrRate</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.EmployerContrRate)
    </div>

    <div class="display-label">EmployeeContrRate</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.EmployeeContrRate)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
