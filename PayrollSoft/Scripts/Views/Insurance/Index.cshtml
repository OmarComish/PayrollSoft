@model IEnumerable<PayrollSoft.Models.Insurance>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            Description
        </th>
        <th>
            ServiceProvider
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ServiceProvider)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.InsuranceCode }) |
            @Html.ActionLink("Details", "Details", new { id=item.InsuranceCode }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.InsuranceCode })
        </td>
    </tr>
}

</table>
